# StasisChess Server

## Prerequisites

- Python >= 3.10
- [uv](https://docs.astral.sh/uv/)

## Usage

- Install uv into your computer
- Use the following commands to run the server
```shell
$ uv sync
$ cp .env.example .env 
# Edit .env file to your settings
$ uv run flask -- run
```